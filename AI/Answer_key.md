
### Answer Key (Questions 01-50)

1. **B** (It learns rules automatically by being fed data and correct answers.)
2. **C** (It has multiple hidden layers in its structure.)
3. **C** (Unsupervised Learning)
4. **C** (Reinforcement Learning)
5. **B** (It uses knowledge from one task to learn another, resulting in faster training and needing less data.)

6. **C** (F1-Score is a balanced measure of precision and recall, ideal for imbalanced data where accuracy can be misleading.)
7. **B** (It calculates blame for each weight using the chain rule so they can be adjusted.)
8. **D** (A large gap between high training accuracy and low test accuracy indicates overfitting, which regularization helps fix.)
9. **C** (ReLU is fast and prevents gradients from becoming too small, solving the vanishing gradient issue caused by Sigmoid.)
10. **A** (Scaling before splitting uses information from the test data to calculate the mean/standard deviation, causing data leakage.)

11. **B** (Validation sets are used during training to tune hyperparameters without touching the final test set.)
12. **C** (Many algorithms, like neural networks and KNN, are sensitive to scale and work better when data is normalized.)
13. **D** (Weights are learned by the model from the data. Learning rate, layers, and batch sizes are hyperparameters set before training.)
14. **C** (Because errors are squared, large deviations resulting from outliers are heavily penalized.)
15. **B** (1,000 total examples ÷ 10 examples per batch = 100 batches per epoch.)
16. **B** (A high learning rate causes steps that are too large, overshooting the minimum and causing the loss to bounce.)
17. **B** (Underfitting occurs when the model is too simple or poorly trained, resulting in low accuracy on both train and test data.)
18. **C** (Dropout randomly deactivates neurons, forcing the network to not rely on any single node and learn redundant representations.)
19. **C** (Recall measures how many actual positive cases were caught, which is critical when false negatives—missing a disease—are costly.)
20. **C** (An AUC of 0.5 represents a diagonal line on the ROC curve, meaning the model is no better than random guessing.)
21. **C** (Despite the name "regression," logistic regression maps outputs to probabilities for binary classification.)
22. **B** (Random forests build many random decision trees and average them, providing better accuracy and preventing the overfitting common in single trees.)
23. **B** (K=1 looks only at the single closest point, making it highly sensitive to noise and resulting in overfitting.)
24. **B** (The kernel trick projects non-linear data into higher dimensions where a linear boundary can separate the classes.)
25. **C** (The elbow method plots the loss against different K values to find the point where adding more clusters yields diminishing returns.)
26. **C** (Pooling layers reduce spatial dimensions by taking the max or average of small regions, keeping the most important information.)
27. **B** (LSTMs use gates to decide what to remember and forget, solving the issue of regular RNNs forgetting early inputs in long sequences.)
28. **B** (The attention mechanism weighs the relevance of all words simultaneously, allowing parallel processing and superior long-term context.)
29. **B** (The discriminator acts as the "judge" trying to detect if the data produced by the generator is real or fake.)
30. **B** (In K=5, the data is split into 5 parts. 4 parts (80%) are used for training, and 1 part (20%) is used for testing in each iteration.)
31. **B** (A zero gradient means the slope is flat, indicating the algorithm has reached a minimum loss point.)
32. **B** (Machine learning algorithms require numerical inputs, so One-Hot Encoding converts text categories into binary vectors.)
33. **B** (PCA combines features to reduce dimensionality, making models faster and visualizations easier while keeping the core variance.)
34. **C** (XGBoost sequentially trains models, focusing on the mistakes of previous models, which is the definition of boosting.)
35. **D** (Softmax maps raw output scores into a normalized probability distribution across multiple classes.)
36. **C** (ReLU has a derivative of 1 for positive numbers, preventing gradients from shrinking to zero, and LSTMs control information flow across sequence steps.)
37. **A** (Once deployed, the real-world data the model sees may shift away from what it was trained on, degrading performance.)
38. **B** (Normalizing before splitting allows statistical information from the test set—like the global mean—to influence the training process.)
39. **C** (A/B testing splits live traffic between two models to see which performs better in the real world.)
40. **B** (Batch norm stabilizes the inputs to layers, reducing internal covariate shift and allowing the network to train faster and more stably.)
41. **C** (Classification only outputs the label, whereas Object Detection outputs both the label and the exact location coordinates (bounding box).)
42. **D** (Tokenization is the foundational NLP step of breaking raw text into individual chunks or tokens.)
43. **B** (Word embeddings capture meaning by mapping words into dense numerical vectors where semantic relationships are preserved mathematically.)
44. **A** (If 99% of data is class A, a model can achieve 99% accuracy by simply guessing class A every time without actually learning.)
45. **C** (Underfitting means the model hasn't learned the basic patterns, leading to poor performance across all datasets.)
46. **A** (L1 applies an absolute value penalty, which pushes less important feature weights exactly to zero, effectively acting as feature selection.)
47. **C** (Reinforcement learning agents learn policies by maximizing cumulative rewards through trial and error.)
48. **B** (Ground truth is the actual, correct label or target that the model is trying to learn to predict.)
49. **C** (Bayesian Optimization builds a probability model of the objective function to intelligently select the next hyperparameters to evaluate.)
50. **C** (Semantic segmentation categorizes every pixel in an image, useful for precise boundaries like identifying tumor pixels in medical scans.)